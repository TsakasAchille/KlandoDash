<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Conducteur du Trajet</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: transparent;
      margin: 0;
      padding: 0;
      font-size: 80%;
    }
    
    .driver-card {
      background: white;
      border-radius: 28px;
      padding: 25px;
      overflow: hidden;
      margin-bottom: 20px;
      min-height: {{ layout.card_min_height if layout else '400px' }};
      height: {{ layout.card_height if layout else '400px' }};
      width: {{ layout.card_width if layout else '100%' }};
    }
    
    .card-header {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
    }
    
    .header-icon {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      background-color: #e6f5eb;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 15px;
      font-size: 20px;
    }
    
    .card-title {
      font-size: 22px;
      font-weight: 600;
      color: #333;
      margin: 0;
    }
    
    .driver-container {
      background: #fafcfe;
      border-radius: 18px;
      padding: 20px;
    }
    
    .driver-header {
      display: flex;
      align-items: center;
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 1px solid #e9ecef;
    }
    
    .driver-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background-color: #e0f0ff;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 15px;
      font-size: 20px;
      color: #3366CC;
      overflow: hidden;
      border: 2px solid #fff;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    
    .driver-avatar img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 50%;
    }
    
    .driver-avatar-fallback {
      font-size: 24px;
    }
    
    .driver-name {
      font-weight: 600;
      font-size: 18px;
      color: #333;
    }
    
    .driver-detail-item {
      display: flex;
      align-items: center;
      margin-bottom: 16px;
      padding-bottom: 16px;
      border-bottom: 1px solid #eaeef3;
    }
    
    .driver-detail-item:last-child {
      margin-bottom: 0;
      padding-bottom: 0;
      border-bottom: none;
    }
    
    .detail-label {
      font-weight: 500;
      color: #666;
      font-size: 13px;
      flex-grow: 1;
    }
    
    .detail-value {
      font-weight: 600;
      color: #1c2b46;
      font-size: 16px;
      padding: 6px 12px;
      background: #f0f4f9;
      border-radius: 12px;
    }
    
    .highlight-value {
      color: #2563eb;
      background: #e0ecff;
    }
    
    .user-id {
      font-family: monospace;
      font-size: 14px;
      background-color: #f0f2f5;
      padding: 2px 6px;
      border-radius: 4px;
      border: 1px solid #e9ecef;
    }
    
    .profile-button {
      margin-top: 15px;
      align-self: flex-end;
      color: #3366CC;
      font-weight: 500;
      background-color: #e0f0ff;
      border-radius: 20px;
      padding: 6px 16px;
      text-decoration: none;
      display: inline-block;
      transition: background-color 0.2s;
      border: none;
      cursor: pointer;
      font-size: 14px;
      font-family: inherit;
    }
    
    .profile-button:hover {
      background-color: #d0e0ff;
    }
    
    .empty-message {
      text-align: center;
      color: #666;
      padding: 40px 20px;
      font-style: italic;
    }
    
    .section {
      margin-bottom: 20px;
    }
    
    .section-title {
      font-size: 16px;
      font-weight: 600;
      color: #333;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
    }
    
    .section-icon {
      margin-right: 8px;
      font-size: 18px;
    }
    
    /* Responsive styling */
    @media (max-width: 768px) {
      .driver-container {
        padding: 10px;
      }
      
      .driver-card-inner {
        padding: 10px;
      }
      
      .driver-card {
        padding: 15px;
      }
    }
  </style>
  <script>
    function viewProfileEvent(userId) {
      window.parent.postMessage({
        type: 'viewProfile',
        userId: userId
      }, '*');
    }
  </script>
</head>
<body>
  <div class="driver-card">
    <div class="driver-container">
      {% if trip and (trip.name or trip.display_name or trip.first_name or trip.email or trip.uid) %}
        <div class="driver-header">
          <div class="driver-avatar">
            {% if trip.photo_url %}
              <img src="{{ trip.photo_url }}" alt="Photo de {{ trip.name or trip.display_name or trip.first_name or 'Conducteur' }}" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
              <div class="driver-avatar-fallback" style="display: none;">👤</div>
            {% else %}
              <div class="driver-avatar-fallback">👤</div>
            {% endif %}
          </div>
          <div class="driver-name">{{ trip.name or trip.display_name or trip.first_name or 'Conducteur' }}</div>
        </div>
          
          {% if config and config.rendering and config.rendering.sections %}
            {% for section in config.rendering.sections %}
              {% if section.fields %}
                {% for field_name in section.fields %}
                  {% set field_config = config.fields.driver_info[field_name] if config.fields and config.fields.driver_info and field_name in config.fields.driver_info else {} %}
                  {% if field_config.display and trip[field_name] %}
                    <div class="driver-detail-item">
                      <div style="flex-grow: 1; display: flex; justify-content: space-between; align-items: center;">
                        <span class="detail-label">{{ field_config.label or field_name.title() }}</span>
                        <span class="detail-value {{ 'highlight-value' if field_name == 'name' else '' }}">{{ trip[field_name] or 'N/A' }}</span>
                      </div>
                    </div>
                  {% endif %}
                {% endfor %}
              {% endif %}
            {% endfor %}
          {% else %}
            <!-- Fallback vers l'affichage simple si pas de config -->
            {% if trip.email %}
              <div class="driver-detail-item">
                <div style="flex-grow: 1; display: flex; justify-content: space-between; align-items: center;">
                  <span class="detail-label">Email</span>
                  <span class="detail-value">{{ trip.email }}</span>
                </div>
              </div>
            {% endif %}
            {% if trip.uid %}
              <div class="driver-detail-item">
                <div style="flex-grow: 1; display: flex; justify-content: space-between; align-items: center;">
                  <span class="detail-label">ID Utilisateur</span>
                  <span class="detail-value user-id">{{ trip.uid }}</span>
                </div>
              </div>
            {% endif %}
            {% if trip.role %}
              <div class="driver-detail-item">
                <div style="flex-grow: 1; display: flex; justify-content: space-between; align-items: center;">
                  <span class="detail-label">Rôle</span>
                  <span class="detail-value">{{ trip.role }}</span>
                </div>
              </div>
            {% endif %}
            {% if trip.phone %}
              <div class="driver-detail-item">
                <div style="flex-grow: 1; display: flex; justify-content: space-between; align-items: center;">
                  <span class="detail-label">Téléphone</span>
                  <span class="detail-value">{{ trip.phone }}</span>
                </div>
              </div>
            {% endif %}
          {% endif %}
          
          {% if trip.uid %}
            <a href="/users?uid={{ trip.uid }}" target="_top" class="profile-button">Voir profil</a>
          {% else %}
            <a href="/users" target="_top" class="profile-button">Voir profil</a>
          {% endif %}
      {% else %}
        <div class="empty-message">
          Aucune information sur le conducteur n'est disponible pour ce trajet.
        </div>
      {% endif %}
    </div>
  </div>
</body>
</html>
