<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Conducteur du Trajet</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: transparent;
      margin: 0;
      padding: 0;
      font-size: 80%;
    }
    
    .driver-card {
      background: white;
      border-radius: 28px;
      padding: 25px;
      overflow: hidden;
      height: {{ layout.card_height if layout else '400px' }};
      width: {{ layout.card_width if layout else '100%' }};
      min-height: {{ layout.card_min_height if layout else '350px' }};
    }
    
    .card-header {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
    }
    
    .header-icon {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      background-color: #e6f5eb;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 15px;
      font-size: 20px;
    }
    
    .card-title {
      font-size: 22px;
      font-weight: 600;
      color: #333;
      margin: 0;
    }
    
    .driver-container {
      background: white;
      border-radius: 18px;
      padding: 20px;
      display: flex;
      flex-direction: column;
      margin-top: 15px;
    }
    
    .driver-card-inner {
      padding: 15px;
      border-radius: 15px;
      background: #fafcfe;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
    }
    
    .driver-header {
      display: flex;
      align-items: center;
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 1px solid #e9ecef;
    }
    
    .driver-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background-color: #e0f0ff;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 15px;
      font-size: 20px;
      color: #3366CC;
    }
    
    .driver-name {
      font-weight: 600;
      font-size: 18px;
      color: #333;
    }
    
    .driver-details {
      list-style-type: none;
      padding: 0;
      margin: 0 0 15px 0;
    }
    
    .driver-detail-item {
      display: flex;
      margin-bottom: 10px;
      align-items: center;
    }
    
    .detail-icon {
      margin-right: 8px;
      width: 20px;
      text-align: center;
    }
    
    .detail-label {
      font-weight: 500;
      color: #666;
      flex-shrink: 0;
      margin-right: 10px;
    }
    
    .detail-value {
      font-weight: 400;
      color: #333;
      word-break: break-word;
      flex-grow: 1;
    }
    
    .user-id {
      font-family: monospace;
      font-size: 14px;
      background-color: #f0f2f5;
      padding: 2px 6px;
      border-radius: 4px;
      border: 1px solid #e9ecef;
    }
    
    .profile-button {
      margin-top: 15px;
      align-self: flex-end;
      color: #3366CC;
      font-weight: 500;
      background-color: #e0f0ff;
      border-radius: 20px;
      padding: 6px 16px;
      text-decoration: none;
      display: inline-block;
      transition: background-color 0.2s;
      border: none;
      cursor: pointer;
      font-size: 14px;
      font-family: inherit;
    }
    
    .profile-button:hover {
      background-color: #d0e0ff;
    }
    
    .empty-message {
      text-align: center;
      color: #666;
      padding: 40px 20px;
      font-style: italic;
    }
    
    .section {
      margin-bottom: 20px;
    }
    
    .section-title {
      font-size: 16px;
      font-weight: 600;
      color: #333;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
    }
    
    .section-icon {
      margin-right: 8px;
      font-size: 18px;
    }
    
    /* Responsive styling */
    @media (max-width: 768px) {
      .driver-container {
        padding: 10px;
      }
      
      .driver-card-inner {
        padding: 10px;
      }
      
      .driver-card {
        padding: 15px;
      }
    }
  </style>
  <script>
    function viewProfileEvent(userId) {
      window.parent.postMessage({
        type: 'viewProfile',
        userId: userId
      }, '*');
    }
  </script>
</head>
<body>
  <div class="driver-card">
    <div class="driver-container">
      {% if trip and (trip.name or trip.display_name or trip.first_name or trip.email or trip.uid) %}
        <div class="driver-card-inner">
          <div class="driver-header">
            <div class="driver-avatar">👤</div>
            <div class="driver-name">{{ trip.name or trip.display_name or trip.first_name or 'Conducteur' }}</div>
          </div>
          
          {% if config and config.rendering and config.rendering.sections %}
            {% for section in config.rendering.sections %}
              {% if section.fields %}
                <div class="section">
                  <div class="section-title" style="color: {{ section.color or '#333' }};">
                    <span class="section-icon">{{ section.icon or '📋' }}</span>
                    {{ section.title or 'Informations' }}
                  </div>
                  
                  <ul class="driver-details">
                    {% for field_name in section.fields %}
                      {% set field_config = config.fields.driver_info[field_name] if config.fields and config.fields.driver_info and field_name in config.fields.driver_info else {} %}
                      {% if field_config.display and trip[field_name] %}
                        <li class="driver-detail-item">
                          <span class="detail-icon" style="color: {{ field_config.icon_color or '#666' }}; font-size: {{ field_config.icon_size or '16px' }};">
                            {{ field_config.icon or '•' }}
                          </span>
                          <span class="detail-label" style="font-size: {{ field_config.label_font_size or '12px' }}; width: {{ field_config.label_width or 'auto' }};">
                            {{ field_config.label or field_name.title() }} :
                          </span>
                          <span class="detail-value {{ field_config.css_class or '' }}" style="font-size: {{ field_config.value_font_size or '14px' }};">
                            {{ trip[field_name] or 'N/A' }}
                          </span>
                        </li>
                      {% endif %}
                    {% endfor %}
                  </ul>
                </div>
              {% endif %}
            {% endfor %}
          {% else %}
            <!-- Fallback vers l'affichage simple si pas de config -->
            <ul class="driver-details">
              {% if trip.email %}
                <li class="driver-detail-item">
                  <span class="detail-icon">📧</span>
                  <span class="detail-label">Email :</span>
                  <span class="detail-value">{{ trip.email }}</span>
                </li>
              {% endif %}
              {% if trip.uid %}
                <li class="driver-detail-item">
                  <span class="detail-icon">🆔</span>
                  <span class="detail-label">ID Utilisateur :</span>
                  <span class="detail-value user-id">{{ trip.uid }}</span>
                </li>
              {% endif %}
              {% if trip.role %}
                <li class="driver-detail-item">
                  <span class="detail-icon">👔</span>
                  <span class="detail-label">Rôle :</span>
                  <span class="detail-value">{{ trip.role }}</span>
                </li>
              {% endif %}
              {% if trip.phone %}
                <li class="driver-detail-item">
                  <span class="detail-icon">📱</span>
                  <span class="detail-label">Téléphone :</span>
                  <span class="detail-value">{{ trip.phone }}</span>
                </li>
              {% endif %}
            </ul>
          {% endif %}
          
          {% if trip.uid %}
            <a href="/users?uid={{ trip.uid }}" target="_top" class="profile-button">Voir profil</a>
          {% else %}
            <a href="/users" target="_top" class="profile-button">Voir profil</a>
          {% endif %}
        </div>
      {% else %}
        <div class="empty-message">
          Aucune information sur le conducteur n'est disponible pour ce trajet.
        </div>
      {% endif %}
    </div>
  </div>
</body>
</html>
